# Contains functions to handle logic (such as adding routes to a database, etc.)

def handle_flight_data(origin, destination):
    # Logic to process the flight data, could be saving to a database
    print(f"Handling flight data: {origin} to {destination}")
    # You can call add_flight_route here to save the data or do other things
